{
    attributes = (
        {
            allowsNull = Y; 
            columnName = "DATAS_PHOTO"; 
            externalType = BLOB; 
            name = datasPhoto; 
            valueClassName = NSData; 
        }, 
        {
            columnName = "NO_INDIVIDU"; 
            externalType = NUMBER; 
            name = noIndividu; 
            valueClassName = NSNumber; 
            valueType = d; 
        }
    ); 
    attributesUsedForLocking = (datasPhoto, noIndividu); 
    className = EOGenericRecord; 
    classProperties = (datasPhoto, noIndividu); 
    externalName = "GRHUM.PHOTOS_EMPLOYES_GRHUM"; 
    fetchSpecificationDictionary = {}; 
    internalInfo = {
        "_clientClassPropertyNames" = (datasPhoto, noIndividu); 
        "_nameInObjectStore" = "PHOTOS_EMPLOYES_GRHUM@BLOBS.world"; 
    }; 
    name = PhotosEmployes; 
    primaryKeyAttributes = (noIndividu); 
}
